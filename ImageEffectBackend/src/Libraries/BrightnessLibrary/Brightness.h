#ifndef BRIGHTNESS_H
#define BRIGHTNESS_H
#include "../Pixel.h"
#include <vector>
using namespace std;
// function declaration
void brightness(vector<vector<Pixel>> &imageVector, float amount);
#endif